# Miscord Docker Configuration
# Copy this file to .env and customize the values

# JWT Secret Key - REQUIRED: Change this to a secure random string (min 32 characters)
JWT_SECRET_KEY=ChangeThisToASecureSecretKeyAtLeast32CharactersLong!

# Server Information
SERVER_NAME=Miscord Server
SERVER_DESCRIPTION=A self-hosted Discord alternative

# Allow new user registration (set to false to disable)
ALLOW_REGISTRATION=true

# Database Configuration
# Set to true for SQLite (simple, single file), false for PostgreSQL (recommended for production)
USE_SQLITE=true

# PostgreSQL password (used when USE_SQLITE=false)
POSTGRES_PASSWORD=miscord
