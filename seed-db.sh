#!/bin/bash

# Database seeding script for development
# This script is no longer needed - users auto-register and communities are auto-created
# by the client application (see MainWindowViewModel.DevModeAutoLoginAsync)
#
# Kept for backward compatibility, but effectively a no-op.

echo "Dev seed script: Users and communities are auto-created by clients"
exit 0
