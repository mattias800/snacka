<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="using:Snacka.Client.Controls"
             xmlns:conv="using:Snacka.Client.Converters"
             xmlns:services="using:Snacka.Client.Services"
             xmlns:vm="using:Snacka.Client.ViewModels"
             xmlns:ui="using:FluentAvalonia.UI.Controls"
             mc:Ignorable="d"
             x:Class="Snacka.Client.Controls.DMContentView"
             x:DataType="controls:DMContentView">

    <Border Background="{StaticResource Content2Brush}">
        <DockPanel>
            <!-- DM Header with close button -->
            <Border DockPanel.Dock="Top" Classes="panel-header">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="@"
                                   Foreground="{StaticResource Foreground3Brush}"
                                   FontSize="22"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding ViewModel.RecipientName, RelativeSource={RelativeSource AncestorType=controls:DMContentView}, FallbackValue='User'}"
                                   Classes="heading-3"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Grid.Column="1"
                            Classes="btn-icon"
                            Command="{Binding ViewModel.CloseCommand, RelativeSource={RelativeSource AncestorType=controls:DMContentView}}"
                            ToolTip.Tip="Close DM">
                        <ui:SymbolIcon Symbol="Dismiss"
                                   Foreground="{StaticResource Foreground3Brush}"
                                   FontSize="16"/>
                    </Button>
                </Grid>
            </Border>

            <!-- DM Message Input -->
            <Border DockPanel.Dock="Bottom" Classes="panel-footer">
                <StackPanel>
                    <!-- DM Typing Indicator -->
                    <TextBlock Text="{Binding ViewModel.TypingIndicatorText, RelativeSource={RelativeSource AncestorType=controls:DMContentView}}"
                               IsVisible="{Binding ViewModel.IsTyping, RelativeSource={RelativeSource AncestorType=controls:DMContentView}}"
                               Classes="text-muted"
                               FontSize="12"
                               FontStyle="Italic"
                               Margin="0,0,0,4"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="DMMessageInputBox"
                                 Grid.Column="0"
                                 Classes="input-chat"
                                 Text="{Binding ViewModel.MessageInput, RelativeSource={RelativeSource AncestorType=controls:DMContentView}, Mode=TwoWay}"
                                 Watermark="{Binding ViewModel.RecipientName, RelativeSource={RelativeSource AncestorType=controls:DMContentView}, StringFormat='Message @{0}'}"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 MaxHeight="200"/>

                        <Button Grid.Column="1"
                                Classes="btn-primary"
                                Command="{Binding ViewModel.SendMessageCommand, RelativeSource={RelativeSource AncestorType=controls:DMContentView}}"
                                Margin="8,0,0,0">
                            <Panel>
                                <TextBlock Text="Send" IsVisible="{Binding ViewModel.IsLoading, RelativeSource={RelativeSource AncestorType=controls:DMContentView}, Converter={x:Static BoolConverters.Not}}"/>
                                <TextBlock Text="..." IsVisible="{Binding ViewModel.IsLoading, RelativeSource={RelativeSource AncestorType=controls:DMContentView}}"/>
                            </Panel>
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- DM Message Edit Overlay -->
            <Border Background="{StaticResource GlassBrush}"
                    DockPanel.Dock="Bottom"
                    Padding="16"
                    CornerRadius="16"
                    Margin="16,0,16,16"
                    IsVisible="{Binding ViewModel.EditingMessage, RelativeSource={RelativeSource AncestorType=controls:DMContentView}, Converter={x:Static ObjectConverters.IsNotNull}}">
                <StackPanel Spacing="8">
                    <TextBlock Text="EDITING MESSAGE"
                               Classes="text-caption"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                 x:Name="EditDMMessageInputBox"
                                 Classes="input-chat"
                                 Text="{Binding ViewModel.EditingMessageContent, RelativeSource={RelativeSource AncestorType=controls:DMContentView}, Mode=TwoWay}"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 MaxHeight="200"/>
                        <Button Grid.Column="1"
                                Classes="btn-secondary"
                                Command="{Binding ViewModel.CancelEditMessageCommand, RelativeSource={RelativeSource AncestorType=controls:DMContentView}}"
                                Content="Cancel"
                                Margin="8,0,0,0"/>
                        <Button Grid.Column="2"
                                Classes="btn-primary"
                                Command="{Binding ViewModel.SaveMessageEditCommand, RelativeSource={RelativeSource AncestorType=controls:DMContentView}}"
                                Margin="8,0,0,0">
                            <Panel>
                                <TextBlock Text="Save" IsVisible="{Binding ViewModel.IsLoading, RelativeSource={RelativeSource AncestorType=controls:DMContentView}, Converter={x:Static BoolConverters.Not}}"/>
                                <TextBlock Text="..." IsVisible="{Binding ViewModel.IsLoading, RelativeSource={RelativeSource AncestorType=controls:DMContentView}}"/>
                            </Panel>
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- DM Messages List -->
            <ScrollViewer x:Name="DMMessagesScrollViewer" Margin="0,8">
                <ItemsControl x:Name="DMMessagesList"
                              ItemsSource="{Binding ViewModel.Messages, RelativeSource={RelativeSource AncestorType=controls:DMContentView}}"
                              Margin="16,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="services:DirectMessageResponse">
                            <controls:MessageItemView
                                Message="{Binding}"
                                AuthorUsername="{Binding SenderUsername}"
                                MessageContent="{Binding Content}"
                                CreatedAt="{Binding CreatedAt}"
                                StartEditMessageCommand="{Binding ViewModel.StartEditMessageCommand, RelativeSource={RelativeSource AncestorType=controls:DMContentView}}"
                                DeleteMessageCommand="{Binding ViewModel.DeleteMessageCommand, RelativeSource={RelativeSource AncestorType=controls:DMContentView}}"
                                ShowThreadIndicator="False"
                                ShowStartThreadButton="False"
                                ShowPinButton="False"
                                ShowReplyButton="False"
                                ShowReactions="False"
                                ShowAttachments="False">
                                <controls:MessageItemView.ShowDateSeparator>
                                    <MultiBinding Converter="{x:Static conv:ShowDateSeparatorConverter.Instance}">
                                        <Binding />
                                        <Binding Path="ViewModel.Messages" RelativeSource="{RelativeSource AncestorType=controls:DMContentView}" />
                                    </MultiBinding>
                                </controls:MessageItemView.ShowDateSeparator>
                            </controls:MessageItemView>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>
    </Border>
</UserControl>
